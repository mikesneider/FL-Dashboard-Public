# Visualización
plotly>=5.0.0
pandas>=1.3.0
numpy>=1.21.0

# Notebook
jupyter>=1.0.0
jupyterlab>=3.0.0
ipython>=7.0.0

# Dashboard (opcional - solo si se ejecuta el servidor Flask)
flask>=2.0.0

# ML (opcional - solo si se re-entrena el modelo)
scikit-learn>=1.0.0

# ──────────────────────────────────────────────────────────────
# ENTRENAMIENTO CON GPU (opcional - solo si quieres re-entrenar)
# ──────────────────────────────────────────────────────────────

# PyTorch - CPU (por defecto)
torch>=2.0.0
torchvision>=0.15.0

# PyTorch - GPU (CUDA 11.8) — descomenta y usa en lugar de las líneas anteriores:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# PyTorch - GPU (CUDA 12.1):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# NVFLARE (framework FL de NVIDIA) - requerido para re-entrenar
nvflare>=2.4.0
